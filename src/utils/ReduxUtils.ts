export const requestType = (action: string) => action + '_REQUEST';
export const responseType = (action: string) => action + '_RESPONSE';
export const errorType = (action: string) => action + '_ERROR';
