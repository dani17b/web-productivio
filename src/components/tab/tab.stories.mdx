import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { Tab } from './Tab';

<Meta title="Components/Content/Tab" component={Tab} />

# Tab

Tabs that contain any of the modules the user might be working with. It allows you 
to navigate through them while editing the modules.

<Props of={Tab} />

export const Template = (args) => (
  <Tab
    tabLabel={args.tabLabel}
    tabContent={args.tabContent}
  />
);

## Basic

<Canvas>
  <Story
    name="Basic"
    args={{
      tabLabel: 'Home',
      tabContent: <Home />,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={FormButton} story="Basic" />